export enum FeedTab {
  LATEST = 'latest',
  POPULAR = 'popular',
}

export enum LoadingSkeletons {
  POST = 'post',
  USER_PROFILE = 'userProfile',
  DISCOVER = 'discover',
}

export enum ContentType {
  POST = 'post',
  COMMENT = 'comment',
}
