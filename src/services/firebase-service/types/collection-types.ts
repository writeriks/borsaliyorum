// TODO: Add collection types here

// Example data type, should be removed later
export interface TestCollection {
  name: string;
}
// Example data enum, should be removed later
export enum TestCollectionEnum {
  NAME = "name",
}
